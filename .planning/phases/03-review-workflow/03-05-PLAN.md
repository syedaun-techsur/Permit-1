---
phase: 03-review-workflow
plan: "05"
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - e2e/phase3-full-lifecycle.spec.ts
  - e2e/phase3-messaging-integration.spec.ts
  - e2e/phase3-notifications.spec.ts
autonomous: true

features:
  implements: ["PERM-05", "PERM-06", "MSG-01", "MSG-02", "MSG-03", "STAT-03", "STAT-04", "STAT-05", "STAT-06", "STAT-07"]
  depends_on: ["PERM-05", "PERM-06", "MSG-01", "MSG-02", "MSG-03", "STAT-03", "STAT-04", "STAT-05", "STAT-06", "STAT-07"]
  enables: []

must_haves:
  truths:
    - "Full reviewer lifecycle E2E test passes: submitted → under_review → additional_info_needed → under_review → approved"
    - "Full reviewer lifecycle E2E test passes: submitted → under_review → rejected"
    - "Messaging integration test: applicant sends message; reviewer receives it; unread count shows on queue page; opening panel clears unread count"
    - "Notification integration test: applicant receives notification when status transitions; clicking notification navigates to correct application"
    - "All Phase 3 E2E tests pass with zero failures in CI (docker compose up)"
    - "Regression: Phase 2 E2E tests still pass after Phase 3 additions"
  artifacts:
    - path: "e2e/phase3-full-lifecycle.spec.ts"
      provides: "Complete reviewer lifecycle E2E covering both approve and reject paths"
      exports: ["full lifecycle tests"]
    - path: "e2e/phase3-messaging-integration.spec.ts"
      provides: "Cross-user messaging: send, receive, unread counts, mark-read on panel open"
      exports: ["messaging integration tests"]
    - path: "e2e/phase3-notifications.spec.ts"
      provides: "Notification creation on lifecycle transitions, panel display, click navigation, mark-all-read"
      exports: ["notification tests"]
  key_links:
    - from: "e2e/phase3-full-lifecycle.spec.ts"
      to: "backend lifecycle action endpoints"
      via: "real HTTP calls through the running docker compose stack"
      pattern: "POST /permits/:id/actions/(begin-review|request-info|respond-to-info|decide)"
    - from: "e2e/phase3-messaging-integration.spec.ts"
      to: "frontend/src/components/messaging/MessagePanel.tsx"
      via: "data-testid selectors: message-bubble, message-composer-input, message-send-button"
      pattern: "data-testid"
    - from: "e2e/phase3-notifications.spec.ts"
      to: "frontend/src/components/notifications/NotificationPanel.tsx"
      via: "data-testid selectors: notification-bell, notification-panel, notification-item"
      pattern: "data-testid"

integration_contracts:
  requires:
    - from_plan: "03-03"
      artifact: "frontend/src/components/messaging/MessagePanel.tsx"
      exports: ["MessagePanel"]
      verify: "grep -n 'export.*MessagePanel' frontend/src/components/messaging/MessagePanel.tsx && echo CONTRACT_OK"
    - from_plan: "03-04"
      artifact: "frontend/src/pages/reviewer/ReviewDetailPage.tsx"
      exports: ["ReviewDetailPage"]
      verify: "grep -n 'ReviewDetailPage' frontend/src/App.tsx && echo CONTRACT_OK"
    - from_plan: "03-04"
      artifact: "frontend/src/pages/reviewer/ReviewQueuePage.tsx"
      exports: ["ReviewQueuePage"]
      verify: "grep -n 'ReviewQueuePage' frontend/src/App.tsx && echo CONTRACT_OK"
  provides: []
---

<objective>
Write comprehensive cross-feature Playwright E2E integration tests for the full Phase 3 feature set: complete reviewer lifecycle (both approve and reject paths), messaging cross-user integration (send/receive/unread counts/mark-read), and notification display (creation on transitions, panel behavior, navigation on click).

Purpose: Plans 03-03 and 03-04 each ship focused E2E tests for their individual features. This plan provides integration-level tests that exercise the complete cross-cutting flows — ensuring the features work together as a system, not just in isolation.

Output: Three E2E spec files covering the complete Phase 3 acceptance criteria as stated in the Phase 3 success criteria. All tests run against the live docker compose stack.
</objective>

<feature_dependencies>
Implements: Integration verification of PERM-05/06, MSG-01/02/03, STAT-03/04/05/06/07 working together as a system
Depends on: All Phase 3 frontend components (03-03, 03-04) and backend APIs (03-01, 03-02)
Enables: None (this is the final verification plan)
</feature_dependencies>

<execution_context>
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/workflows/execute-plan.md
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-workflow/03-03-PLAN.md
@.planning/phases/03-review-workflow/03-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full lifecycle + reject path E2E tests</name>
  <files>
    e2e/phase3-full-lifecycle.spec.ts
  </files>
  <action>
Create `e2e/phase3-full-lifecycle.spec.ts` — comprehensive reviewer lifecycle tests using the real running stack (docker compose up).

**Test setup pattern** (use `test.beforeAll` for speed — create users once per suite):
- Create users via the backend API directly using `request.newContext()` from Playwright:
  - POST /auth/register → applicant (email: `e2e-applicant-p3@test.com`, password: `TestPass123!`, role: `applicant`)
  - POST /auth/register → reviewer (email: `e2e-reviewer-p3@test.com`, password: `TestPass123!`, role: `reviewer`)
- Login both, store JWT tokens
- Create + submit a permit application as the applicant (POST /permits + POST /permits/:id/submit)
- Store `applicationId` and `referenceNumber` for use in all tests

Use two browser contexts: `applicantContext` + `reviewerContext` to avoid cookie conflicts.

**Describe block: "Full Approve Lifecycle"**

```
Test 1: ReviewQueuePage shows submitted application
- reviewerContext: navigate to /review/queue
- Assert h1 contains "Review Queue"
- Assert: row with referenceNumber visible in the table
- Assert: status badge shows "Submitted"

Test 2: Reviewer begins review
- Click application row → navigates to /review/:id
- Wait for data-testid="begin-review-btn" to be visible
- Click "Begin Review"
- Assert: data-testid="request-info-btn" is now visible (confirms transition to under_review)
- Assert: status badge in the page shows "Under Review"

Test 3: Reviewer requests additional information
- Click data-testid="request-info-btn"
- Assert: data-testid="request-info-modal" visible
- Fill textarea with "Please provide an updated site plan showing all drainage channels."
- Click "Send Request"
- Assert: modal closes; status badge shows "Additional Info Needed"
- Assert: infoRequestNote text visible somewhere on the page

Test 4: Applicant sees information request on their detail page
- applicantContext: navigate to /applications/:id
- Assert: data-testid="respond-to-info-section" is visible
- Assert: "Please provide an updated site plan..." text is visible within the section

Test 5: Applicant submits response
- Fill data-testid="respond-textarea" with "I have attached the updated site plan."
- Click data-testid="submit-response-btn"
- Assert: success toast visible containing "back under review"
- Assert: data-testid="respond-to-info-section" is no longer visible (status changed)

Test 6: Reviewer approves application
- reviewerContext: refresh /review/:id
- Assert: data-testid="approve-btn" visible
- Click "Approve"
- Assert: DecisionModal visible
- Fill reason field with "Application meets all zoning requirements and drainage standards."
- Click green "Approve" button
- Assert: status badge shows "Approved"
- Assert: decision details card visible with the reason text
- Assert: data-testid="approve-btn" NOT in DOM (read-only state)
```

**Describe block: "Reject Lifecycle"** (separate describe, uses a new permit):
```
Test 7: Reviewer submits application, begins review, and rejects
- Create second permit as applicant + submit
- Reviewer: begin-review on second permit
- Click data-testid="reject-btn"
- Assert: "Are you sure?" text visible in modal
- Fill reason "The site does not comply with setback requirements per section 4.2."
- Click red "Reject Application" button
- Assert: status badge shows "Rejected"
- Assert: read-only decision card shows "rejected" and reason text

Test 8: Rejected application cannot be acted upon again
- Verify action panel shows no action buttons (only read-only decision details)
```

**Teardown**: After all tests, clean up test data by calling DELETE /admin/test-cleanup or use a test-only endpoint. If no cleanup endpoint exists, prefix all test data with `e2e-` and document manual cleanup steps.
  </action>
  <verify>
```bash
npx playwright test e2e/phase3-full-lifecycle.spec.ts --reporter=list 2>&1 | tail -40 && echo "LIFECYCLE E2E PASSED"
```
  </verify>
  <done>
- phase3-full-lifecycle.spec.ts exists with 8 named tests in 2 describe blocks
- All tests pass: approve path (Tests 1-6) + reject path (Tests 7-8)
- Status badge transitions verified at each step
- Action panel state verified (buttons present/absent based on status)
- Decision details card visible after terminal decision
  </done>
</task>

<task type="auto">
  <name>Task 2: Messaging integration + notification E2E tests</name>
  <files>
    e2e/phase3-messaging-integration.spec.ts
    e2e/phase3-notifications.spec.ts
  </files>
  <action>
**`e2e/phase3-messaging-integration.spec.ts`** — cross-user messaging integration:

Setup: same user pair and submitted permit as plan-level tests. Reviewer begins review (so messaging is active — status is under_review). Two browser contexts: `applicantPage` + `reviewerPage`.

```
Test 1: Applicant sends a message
- applicantPage: navigate to /applications/:id
- Locate data-testid="message-composer-input" (in MessagePanel)
- Type "Hello, I have submitted my initial documents."
- Press Enter
- Assert: data-testid="message-bubble" appears with "Hello, I have submitted..." text
- Assert: bubble is right-aligned (applicant's own message)
- Assert: bubble contains applicant's name and "applicant" role badge

Test 2: Reviewer receives the message (30s poll or manual page refresh)
- reviewerPage: navigate to /review/:id
- Wait up to 35s for data-testid="message-bubble" to appear (accounts for 30s poll)
- Assert: bubble is left-aligned (from applicant's perspective this would be right; from reviewer's it's left)
- Assert: contains applicant's name

Test 3: Unread count badge on ReviewQueuePage
- Log out reviewer and log back in (fresh session so unread count is loaded)
- Navigate to /review/queue
- Assert: the application row has a message count badge showing "1" (or > 0)

Test 4: Opening MessagePanel marks messages as read
- reviewerPage: navigate to /review/:id
- Assert: MessagePanel mounts and POST /messages/:msgId/read is called (use page.waitForRequest)
- Navigate to /review/queue
- Assert: unread badge on that application row is gone (or shows 0)

Test 5: Reviewer sends a message with Shift+Enter creating a newline
- reviewerPage: on /review/:id, click in composer
- Type "Line one", then Shift+Enter, then "Line two"
- Press Enter to send
- Assert: message bubble contains both lines (not sent prematurely on Shift+Enter)

Test 6: Messaging blocked on draft application
- Create a new permit without submitting (stays in draft)
- applicantPage: navigate to /applications/:draftId
- Assert: MessagePanel is NOT rendered (or shows "Messaging unavailable for draft applications")

Test 7: Reviewer attach button visible; applicant attach button not visible
- reviewerPage: on /review/:id, assert paperclip button (data-testid="attach-btn") visible in composer
- applicantPage: on /applications/:id, assert paperclip button NOT in DOM
```

---

**`e2e/phase3-notifications.spec.ts`** — notification creation, display, and navigation:

Setup: same user pair + submitted permit. Use `applicantPage` and `reviewerPage` contexts.

```
Test 1: Applicant receives notification when review begins
- reviewerPage: POST /permits/:id/actions/begin-review (via fetch or page.evaluate)
- applicantPage: click data-testid="notification-bell" in NavBar
- Assert: data-testid="notification-panel" appears
- Assert: notification item containing "now under review" + referenceNumber visible

Test 2: Notification badge count in NavBar
- applicantPage: navigate to /applications (list page) without opening notification panel
- Assert: NavBar bell badge shows count > 0 (the "now under review" notification)
- Click bell → panel opens
- Assert: badge disappears after viewing (handled by marking as read on click)

Test 3: Clicking notification navigates to correct application
- applicantPage: open NotificationPanel; click the "now under review" notification item
- Assert: URL changes to /applications/:id (the correct application)
- Assert: notification panel closes after navigation

Test 4: Mark all as read
- Trigger another status change to create a new notification:
  reviewerPage: POST /permits/:id/actions/request-info with note "Test info request"
- applicantPage: click bell → panel opens showing new notification
- Click "Mark all as read" link
- Assert: all notification items have read styling (e.g., no unread indicator dot)
- Assert: NavBar bell badge count drops to 0

Test 5: Reviewer receives notification when applicant responds
- applicantPage: respond-to-info on the application
- reviewerPage: click bell icon
- Assert: notification item containing "Applicant has responded" + referenceNumber visible

Test 6: Notification for new message
- applicantPage: send a message on the application
- reviewerPage: wait for notification bell badge to update (30s poll)
- Assert: NavBar badge count increases
- Assert: NotificationPanel notification item contains "New message on application"
```

**Common test utilities**: Both specs should share a `e2e/helpers/auth.helper.ts` (or use the same pattern from phase3-full-lifecycle.spec.ts) for:
- `loginAs(page, email, password)` → sets auth token in localStorage/sessionStorage to match what the frontend expects
- `createAndSubmitPermit(apiRequestContext, applicantToken)` → creates + submits a permit, returns applicationId

If `auth.helper.ts` doesn't already exist in the e2e directory, create it here.
  </action>
  <verify>
```bash
npx playwright test e2e/phase3-messaging-integration.spec.ts --reporter=list 2>&1 | tail -40 && echo "MESSAGING E2E PASSED"
```
```bash
npx playwright test e2e/phase3-notifications.spec.ts --reporter=list 2>&1 | tail -40 && echo "NOTIFICATIONS E2E PASSED"
```
```bash
# Regression: Phase 2 tests still pass
npx playwright test e2e/responsive.spec.ts e2e/interactive-states.spec.ts --reporter=list 2>&1 | tail -20 && echo "PHASE2 REGRESSION PASSED"
```
  </verify>
  <done>
- phase3-messaging-integration.spec.ts: 7 tests — send (applicant), receive (reviewer), unread count badge, mark-read on panel open, Shift+Enter newline, draft messaging blocked, attach button role visibility
- phase3-notifications.spec.ts: 6 tests — notification created on begin-review, badge count, click-to-navigate, mark-all-read, reviewer notification on applicant response, new message notification
- auth.helper.ts created if needed with loginAs() and createAndSubmitPermit() utilities
- All 19 Phase 3 E2E tests pass (7 from messaging, 6 from notifications, 8 from lifecycle, minus any skipped CI-environment tests)
- Phase 2 regression tests (responsive.spec.ts, interactive-states.spec.ts) still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→API | E2E test helper scripts POST to /auth/register and /permits — these endpoints have existing RBAC from Phase 1/2. Test accounts are created with known credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Information disclosure | E2E test credentials hardcoded in spec files | accept | Test credentials (`e2e-applicant-p3@test.com`, `TestPass123!`) are used only in test/CI environments against the local docker compose stack; no production system involved. Risk accepted — owned by test infrastructure, not production. |
| T-03-25 | Tampering | E2E test data polluting shared database | mitigate | All test users use `e2e-*` email prefix; `test.beforeAll` creates isolated data per test suite; `test.afterAll` or documented manual cleanup removes e2e-prefixed accounts; integration tests in plan 03-01/02 use Jest's in-memory test database (not shared postgres) |
</threat_model>

<verification>
1. `npx playwright test e2e/phase3-full-lifecycle.spec.ts --reporter=list` — 8/8 pass
2. `npx playwright test e2e/phase3-messaging-integration.spec.ts --reporter=list` — 7/7 pass
3. `npx playwright test e2e/phase3-notifications.spec.ts --reporter=list` — 6/6 pass
4. `npx playwright test e2e/responsive.spec.ts e2e/interactive-states.spec.ts --reporter=list` — Phase 2 regression passes (0 failures)
5. Total Phase 3 E2E tests: 21 tests across 3 spec files + 8 from lifecycle = all pass
</verification>

<success_criteria>
- Full lifecycle E2E: approve path (6 steps) and reject path (2 steps) both pass end-to-end
- Messaging cross-user: send message (applicant) → receive (reviewer) → unread count on queue page → mark-read on panel open
- Notification E2E: created on begin-review, visible in NotificationPanel, click navigates correctly, mark-all-read clears badge
- Phase 2 regression: responsive and interactive-state tests still pass (no regressions from Phase 3 changes)
- All Phase 3 Playwright tests pass in the docker compose environment
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-workflow/03-05-SUMMARY.md`
</output>
